const ExampleSharedComponent = () => (
    <div>
        <h1>Example Shared Component</h1>
        <p>This component is usable in both editor and frontend contexts.</p>
    </div>
);

export default ExampleSharedComponent;
